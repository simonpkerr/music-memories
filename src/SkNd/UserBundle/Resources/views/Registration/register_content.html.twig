{#% form_theme form theme %#}

<h1>Sign up</h1>
<div class="half-width fl">
    <p class="strapline fl">Why should I sign up? Here&rsquo;s 3 good reasons&hellip;</p>
    <img src="{{ asset("bundles/SkNd/images/rediscoverIt.jpg") }}" alt="noodleDig image" />
<ul class="bullet-list">
    <li>You&rsquo;ll get more personalised recommendations when using noodleDig, allowing you find more relevant stuff.</li>
    <li>You&rsquo;ll get to create your very own personalised &lsquo;memory walls&rsquo; and then add all manner of cool stuff to them.</li>
    <li>You&rsquo;ll have the warm inner feeling that comes from knowing you&rsquo;re a true noodleDigg&ndash;er.</li>
</ul>
<p class="strapline">So, go on, sign up &ndash; its really very simple.</p>
</div>

<div class="fr half-width">
    <p class="strapline">Use the form below to sign up to noodleDig in the usual way.</p>

<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
<fieldset>
    <legend>Fill in some details</legend>
    {#{ form_widget(form) }#}
    
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.dateofbirth) }}    
    {{ form_row(form.firstname) }}
    {{ form_row(form.surname) }}
    {{ form_row(form.plainPassword) }}
    <p>I agree to <a href="{{ path('tac') }}" target="_blank">noodleDig&rsquo;s terms and conditions</a>.</p>
    {{ form_row(form.tacagreement) }}
    
    {{ form_rest(form) }}
    
    <div>
        <input  type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</fieldset>
</form>
</div>

{#<div class="fl half-width">
    <h2>Facebook sign up</h2>
    <p>Alternatively, you can sign up for noodleDig using your Facebook account, saving you the hassle of filling out yet another form and remembering more passwords!</p>
        
</div>#}
